import React, { useState } from 'react';
import Members from '../contexts/user-data.json';

const Table = () => {
  const [members, setMembers] = useState([])

  return (
    <div>Table</div>
  )
}

export default Table